#include <unistd.h>
#include <stdlib.h>
#include <string.h> // strlen 함수 사용을 위해 사용한 함수 -> 나중에 지워야함
#include <stdio.h>

char *strjoin(const char *s1, const char *s2);
char *ft_get_userid(char **env);

