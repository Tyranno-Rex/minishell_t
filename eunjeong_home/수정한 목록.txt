1. 파일디스크립터 열고 닫는 함수 추가함 : builtins 파일에 redir_fd.c 파일 추가함

2. execve 함수에 env_2D 변수 필요해서 추가함 : Pipe 파일에 make_env2D.c 파일에 추가함. + sturct.h 파일에 t_glob 구조체에 char **env_2D 추가함. + pipe.h 파일에 해당 함수 추가함.

3. 자식 프로세스 생성(PIPE 처리하면서 생성되야함 + execve 함수 실해될 때도 실행되어야 함.) : PIPE 파일에  create_child.c(해당 파일에서만 작업) 파일 생성해 둠 이외 다른 파일은 건들지 않음.
